<p id="notice"><%= notice %></p>

<div class="container">

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Vlog
                <small>My bootcamp journey</small>
            </h1>
        </div>
    </div>
    <%= link_to 'New Vlog', new_vlog_path %>

    <div class="row">
      <% @vlogs.each do |vlog| %>
        <div class="col-xs-12 col-sm-6 col-md-4 portfolio-item">
            <div class="card" style="width: 20rem;">
              <%= image_tag(vlog.avatar_url.to_s) %>
              <div class="card-block">
                <h4 class="card-title"> <%= vlog.title %></h4>
                <p class="card-text"><%= vlog.description %></p>
                <p class="card-text"><%= vlog.youtube_link %></p>
                <a href="#" class="btn btn-primary">Rating</a>
                <%= link_to 'Show', vlog %>
                <%= link_to 'Edit', edit_vlog_path(vlog) %>
                <%= link_to 'Destroy', vlog, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </div>
            </div>
          </div>
            <% end %>

        </div>
    </div>




    <hr>

    <!-- Pagination -->
    <nav aria-label="...">
      <ul class="pagination">
        <li class="page-item">
          <a class="page-link" href="#" tabindex="-1">Previous</a>
        </li>
        <li class="page-item active"><a class="page-link" href="#">1</a></li>
        <li class="page-item">
          <a class="page-link" href="#">2 <span class="sr-only">(current)</span></a>
        </li>
        <li class="page-item"><a class="page-link" href="#">3</a></li>
        <li class="page-item">
          <a class="page-link" href="#">Next</a>
        </li>
      </ul>
    </nav>
    <hr>

</div>
