<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @topic.title %>
</p>

<%= image_tag(@topic.avatar_url.to_s, class: 'smallies') %>


<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %> |
<%= link_to 'Delete', @topic, method: :delete, data: { confirm: 'Are you sure?' } %>
